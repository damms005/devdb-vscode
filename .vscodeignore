# Ignore everything by default
*
**

# Include only necessary files
!package.json
!LICENSE*

# Include source and compiled files
!*.js
!dist/**/*.js
!ui-shell/dist/**

# Include icons
!resources/devdb-128x128.png
!resources/devdb.png

# Include snippets and schemas
!snippets/*
!schemas/*

# Include @vscode/sqlite3 because we excluded it from build in esbuild.js
!node_modules/@vscode/sqlite3/**